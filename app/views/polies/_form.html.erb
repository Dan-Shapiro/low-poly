<%= form_for @poly, html: { multipart: true } do |f| %>
  <% if @poly.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poly.errors.count, "error") %> prohibited this poly from being saved:</h2>

      <ul>
      <% @poly.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
